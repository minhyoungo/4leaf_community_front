import React from "react";
import styled from "styled-components";
import { withResizeDetector } from "react-resize-detector";
import CircularIndeterminate from "../../Components/loading/CircularIndeterminate";
import {
  EmptyList,
  SearchWrapper,
  TableBody,
  TableBodyLIST,
  TableWrapper,
  Wrapper,
  WholeWrapper,
  TextInput,
  TableHead,
  TableHeadLIST,
  PagenationWrapper,
  PagenationBtn,
  Pagenation,
  MobileTable,
  MobileTableWrapper,
  RsWrapper,
  CommonButton,
} from "../../../Components/CommonComponents";

const MM10Presenter = ({ noticeDatum }) => {
  return <WholeWrapper>gd</WholeWrapper>;
};
export default withResizeDetector(MM10Presenter);
